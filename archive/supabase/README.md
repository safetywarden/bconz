# Archived Supabase Migration

`20260805_create_contact_enquiries.sql` was part of an abandoned contact-enquiry database experiment.

It is retained only for historical reference. The production BCONZ contact form now uses Web3Forms and does not use Supabase, SMTP, Nodemailer, or a contact-enquiry database at runtime.
