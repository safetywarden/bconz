export type NavItem = {
  title: string;
  href: string;
};

export type FooterLink = {
  title: string;
  href: string;
};

export type LogoConfig = {
  logoPath: string;
  markPath: string;
  logoAltText: string;
  markAltText: string;
};
